CREATE TABLE bot_survey_questions (
  question_id int not null auto_increment
, question_text varh(1024) not null
, primary key (question_id)
);

CREATE TABLE survey_branching_logic (
  question_id
  magic_text (e.g. "Los Angeles")
  action_to_take
)

CREATE TABLE bot_survey_responses (
)

CREATE TABLE bot_survey_response_answers (
)


If yes, Go to 5, Else go to 4

if answer.find('yes') > 0:
  go_to_question(5)


If Los Angeles, mention Data Skeptic records in Los Angeles.  If Chicago, mention Kyle used to live there.  If North Carolina, mention Linhda used to live there. Go to 6

answer = "I live in Los Angeles"
if answer.find('Los Angeles') > 0:
  print("That is where the show is recorded")


If has "commute" Go to 10, otherwise go to 11